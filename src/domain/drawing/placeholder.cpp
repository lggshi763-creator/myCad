// Placeholder so mycad_drawing has at least one translation unit to link.
// Real sources land in Phase 1.C (Drawing aggregate behaviors) and
// Phase 1.E (BOM generator). Feel free to delete once real .cpp files exist.

namespace mycad::domain::drawing {}
